import Foundation
import CoreGraphics

struct UIConstants {
    // MARK: - Global 8pt Spacing Grid
    // Enforcing strict rhythmic layout logic across all screens.
    
    struct Spacing {
        /// 8 pt
        static let xs: CGFloat = 8
        /// 16 pt
        static let sm: CGFloat = 16
        /// 24 pt
        static let md: CGFloat = 24
        /// 32 pt
        static let lg: CGFloat = 32
        /// 48 pt
        static let xl: CGFloat = 48
    }
}
